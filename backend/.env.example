# Fill this with your MongoDB connection string
MONGODB_URI="connection_string_here"

# Optional port (defaults to 3000)
PORT=3000

# JWT secret for signing auth tokens
JWT_SECRET="replace_with_strong_secret"

# Frontend origin for CORS (include protocol and port)
CLIENT_ORIGIN="http://localhost:5173"
